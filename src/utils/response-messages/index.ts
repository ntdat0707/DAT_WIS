import { generalErrorDetails, buildErrorDetail } from './error-details';
import { buildSuccessMessage, buildErrorMessage, IErrorDetail, ISourceError, isPaginationResult } from './responses';
export {
  buildSuccessMessage,
  buildErrorMessage,
  IErrorDetail,
  generalErrorDetails,
  buildErrorDetail,
  ISourceError,
  isPaginationResult
};
